node_modules/
dist/
*.part
# Sende-Token fuer den Diagnose-/Fehler-Versand - DARF NIE ins Repo.
# Es wird nur in den Installer eingepackt (bewusster, dokumentierter Kompromiss),
# aber ein Token in der Git-Historie waere fuer immer oeffentlich.
telemetrie.json

# Sitzungs-Arbeitsbaeume und maschinenlokale Einstellungen. Standen bisher nur in
# .git/info/exclude - also nur auf diesem einen Rechner.
.claude/worktrees/
.claude/settings.local.json
release-baut.json

# Obsidian-Tresor-Einstellungen (Wilhelm liest das Wiki in Obsidian) - maschinenlokal.
.obsidian/
wiki/.obsidian/
